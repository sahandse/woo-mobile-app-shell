name: Build woo-mobile-app-shell Installable ZIP

on:
  push:
    branches: [main]
    paths:
      - '**/*.php'
      - 'assets/**'
      - 'includes/**'
      - 'languages/**'
      - 'readme.txt'
      - 'README.md'
      - '.github/workflows/build-release.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Read version
        id: version
        shell: bash
        run: |
          VERSION=$(grep 'Version:' woo-mobile-app-shell.php | head -1 | sed 's/.*Version:[[:space:]]*//')
          test -n "$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: PHP syntax check
        shell: bash
        run: |
          set -e
          find . -type f -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l
      - name: Build ZIP
        shell: bash
        run: |
          rm -rf dist build
          mkdir -p dist build/woo-mobile-app-shell
          rsync -a --exclude='.git' --exclude='.github' --exclude='build' --exclude='dist' ./ build/woo-mobile-app-shell/
          cd build
          zip -qr ../dist/woo-mobile-app-shell.zip woo-mobile-app-shell
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: woo-mobile-app-shell-v${{ steps.version.outputs.version }}
          path: dist/woo-mobile-app-shell.zip
      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          TAG="woo-mobile-app-shell-v${VERSION}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/woo-mobile-app-shell.zip --clobber
          else
            gh release create "$TAG" dist/woo-mobile-app-shell.zip --title "woo-mobile-app-shell v${VERSION}" --notes "Installable WordPress ZIP for woo-mobile-app-shell v${VERSION}."
          fi
